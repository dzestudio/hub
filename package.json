{
  "name": "hub",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2024-09-23",
    "deploy": "wrangler pages deploy . --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^3.101.0"
  }
}
